{
    "name": "coderkearns.github.io",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "deploy": "pnpm run build && gh-pages -d dist -b gh-pages"
    },
    "devDependencies": {
        "gh-pages": "^4.0.0",
        "vite": "^3.0.0"
    }
}
